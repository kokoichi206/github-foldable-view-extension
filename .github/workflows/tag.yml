name: tag

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  tag:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      tag: ${{ steps.create.outputs.tag }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # GITHUB_TOKEN で push したタグは workflow を起こさないため、
      # release は push トリガーではなく下の job から workflow_call で呼ぶ
      - name: Create and push tag
        id: create
        run: |
          VERSION=$(jq -r .version package.json)
          TAG="v${VERSION}"
          if git rev-parse "$TAG" >/dev/null 2>&1; then
            echo "::error::Tag $TAG already exists"
            exit 1
          fi
          git tag "$TAG"
          git push origin "$TAG"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"

  release:
    needs: tag
    uses: ./.github/workflows/release.yml
    with:
      tag: ${{ needs.tag.outputs.tag }}
    secrets: inherit
